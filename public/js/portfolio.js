const handlePredictionHistory = (event) => {
    event.preventDefault();

    //want to grab data from the GET
    // display the most recent three requests on the page

}

document.addEventListener("onload", handlePredictionHistory);